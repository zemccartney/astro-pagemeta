pre-commit:
    parallel: true
    commands:
        lint:
            glob: "**/*.{ts,astro,json}"
            run: pnpm lint:fix
            stage_fixed: true
        fmt:
            run: pnpm fmt:fix
            stage_fixed: true
        typecheck:
            glob: "**/*.{ts,astro}"
            run: pnpm typecheck
